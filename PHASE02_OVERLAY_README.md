# EvidenceGap V1 Phase 02 Overlay

This archive is rooted at the repository root. Copy or rsync its contents into
an EvidenceGap checkout after Phase 01 has completed successfully.

Primary entry point:

```bash
python scripts/run_v1_phase02.py --help
```

See `docs/v1/phase02_usage.md` for the recommended quick and full workflows.
